# 后端小程序码生成接口实现

> 参考文档：[uniapp生成二维码，以及扫码进入另一个小程序某个页面](https://blog.csdn.net/m0_51431448/article/details/144485864)

## 🎯 核心问题

当前生成的是普通文本二维码，扫码后显示文本内容，不会自动跳转到小程序。需要调用微信官方接口生成真正的小程序码。

## 📋 微信官方小程序码生成接口

### 1. 获取access_token

```java
@Component
public class WechatAccessTokenService {
    
    @Value("${wechat.appid}")
    private String appId;
    
    @Value("${wechat.secret}")
    private String appSecret;
    
    private String accessToken;
    private long expireTime;
    
    public String getAccessToken() {
        // 检查token是否过期
        if (accessToken != null && System.currentTimeMillis() < expireTime) {
            return accessToken;
        }
        
        // 获取新的access_token
        String url = "https://api.weixin.qq.com/cgi-bin/token" +
                    "?grant_type=client_credential" +
                    "&appid=" + appId +
                    "&secret=" + appSecret;
        
        try {
            RestTemplate restTemplate = new RestTemplate();
            Map<String, Object> response = restTemplate.getForObject(url, Map.class);
            
            if (response.containsKey("access_token")) {
                this.accessToken = (String) response.get("access_token");
                int expiresIn = (Integer) response.get("expires_in");
                this.expireTime = System.currentTimeMillis() + (expiresIn - 300) * 1000L; // 提前5分钟过期
                
                return this.accessToken;
            } else {
                throw new RuntimeException("获取access_token失败: " + response.get("errmsg"));
            }
        } catch (Exception e) {
            throw new RuntimeException("调用微信API失败", e);
        }
    }
}
```

### 2. 生成小程序码接口

```java
@RestController
@RequestMapping("/butler")
public class QrCodeController {
    
    @Autowired
    private WechatAccessTokenService accessTokenService;
    
    /**
     * 生成小程序码
     * 参考：https://developers.weixin.qq.com/miniprogram/dev/api-backend/open-api/qr-code/wxacode.getUnlimited.html
     */
    @GetMapping("/generateMiniProgramCode")
    public ResponseEntity<?> generateMiniProgramCode(
            @RequestParam String phone,
            @RequestParam(required = false) String community,
            @RequestParam(required = false) String province,
            @RequestParam(required = false) String city,
            @RequestParam(required = false) String district,
            @RequestParam(required = false) String building,
            @RequestParam(required = false) String units,
            @RequestParam(required = false) String floor,
            @RequestParam(required = false) String room) {
        
        try {
            // 1. 获取access_token
            String accessToken = accessTokenService.getAccessToken();
            
            // 2. 构建场景值（简化参数避免32字符限制）
            String scene = buildScene(phone, community, province, city, district, building, units, floor, room);
            
            // 3. 调用微信API生成小程序码
            String wechatUrl = "https://api.weixin.qq.com/wxa/getwxacodeunlimit?access_token=" + accessToken;
            
            Map<String, Object> params = new HashMap<>();
            params.put("scene", scene);
            params.put("page", "pages/auth/visitor-apply");  // 访客申请页面
            params.put("width", 430);
            params.put("auto_color", false);
            params.put("line_color", Map.of("r", 0, "g", 0, "b", 0));
            params.put("is_hyaline", false);
            
            // 4. 发送请求获取小程序码图片
            byte[] imageBytes = callWechatMiniProgramApi(wechatUrl, params);
            
            // 5. 转换为Base64返回
            String base64Image = "data:image/png;base64," + Base64.getEncoder().encodeToString(imageBytes);
            
            // 6. 构建返回数据
            Map<String, Object> resultData = new HashMap<>();
            resultData.put("type", "wechat_mini_program");
            resultData.put("qrCodeImage", base64Image);
            resultData.put("officialCode", true);
            resultData.put("scene", scene);
            resultData.put("pagePath", "pages/auth/visitor-apply");
            resultData.put("butlerPhone", phone);
            resultData.put("butlerName", getButlerNameByPhone(phone));
            resultData.put("timestamp", System.currentTimeMillis());
            
            return ResponseEntity.ok(Map.of("code", "0", "data", resultData));
            
        } catch (Exception e) {
            log.error("生成小程序码失败", e);
            return ResponseEntity.ok(Map.of("code", "500", "msg", "生成失败：" + e.getMessage()));
        }
    }
    
    /**
     * 构建场景值（最大32字符限制）
     */
    private String buildScene(String phone, String community, String province, 
                             String city, String district, String building, 
                             String units, String floor, String room) {
        StringBuilder scene = new StringBuilder();
        
        // 使用简化参数名
        scene.append("bp=").append(phone.substring(Math.max(0, phone.length() - 8))); // 后8位手机号
        
        if (StringUtils.hasText(community)) {
            scene.append("&c=").append(URLEncoder.encode(community.length() > 4 ? 
                community.substring(0, 4) : community, StandardCharsets.UTF_8));
        }
        
        if (StringUtils.hasText(building)) {
            scene.append("&b=").append(building);
        }
        
        if (StringUtils.hasText(units)) {
            scene.append("&u=").append(units);
        }
        
        if (StringUtils.hasText(floor)) {
            scene.append("&f=").append(floor);
        }
        
        if (StringUtils.hasText(room)) {
            scene.append("&r=").append(room);
        }
        
        // 确保不超过32字符
        String result = scene.toString();
        if (result.length() > 32) {
            // 如果超长，只保留关键信息
            result = "bp=" + phone.substring(Math.max(0, phone.length() - 8));
            if (StringUtils.hasText(community)) {
                String shortCommunity = community.length() > 4 ? community.substring(0, 4) : community;
                if ((result + "&c=" + shortCommunity).length() <= 32) {
                    result += "&c=" + shortCommunity;
                }
            }
        }
        
        return result;
    }
    
    /**
     * 调用微信小程序码生成API
     */
    private byte[] callWechatMiniProgramApi(String url, Map<String, Object> params) throws Exception {
        RestTemplate restTemplate = new RestTemplate();
        
        HttpHeaders headers = new HttpHeaders();
        headers.setContentType(MediaType.APPLICATION_JSON);
        
        HttpEntity<Map<String, Object>> entity = new HttpEntity<>(params, headers);
        
        ResponseEntity<byte[]> response = restTemplate.exchange(
            url, HttpMethod.POST, entity, byte[].class);
        
        if (response.getStatusCode() == HttpStatus.OK) {
            byte[] result = response.getBody();
            
            // 检查返回的是否是错误JSON而不是图片
            if (result != null && result.length > 0) {
                // 简单检查是否是JSON错误响应
                String resultStr = new String(result, StandardCharsets.UTF_8);
                if (resultStr.startsWith("{") && resultStr.contains("errcode")) {
                    throw new RuntimeException("微信API返回错误: " + resultStr);
                }
            }
            
            return result;
        } else {
            throw new RuntimeException("调用微信API失败，状态码: " + response.getStatusCodeValue());
        }
    }
    
    /**
     * 根据手机号获取管家姓名
     */
    private String getButlerNameByPhone(String phone) {
        // 这里应该查询数据库获取管家姓名
        // 暂时返回默认值
        return "管家";
    }
}
```

### 3. 配置文件

```yaml
# application.yml
wechat:
  appid: your_mini_program_appid
  secret: your_mini_program_secret
```

## 🔄 前端处理优化

### 1. 优化二维码生成逻辑

```javascript
// 在 qrcode-generator.vue 中优化生成逻辑
async generateMiniProgramCode() {
    try {
        const response = await request({
            url: apiUrls.butler.generateMiniProgramCode,
            method: 'GET',
            data: {
                phone: this.butlerInfo.phone,
                community: this.selectedAddress.community,
                province: this.selectedAddress.province,
                city: this.selectedAddress.city,
                district: this.selectedAddress.district,
                building: this.selectedAddress.building,
                units: this.selectedAddress.units,
                floor: this.selectedAddress.floor,
                room: this.selectedAddress.room
            }
        });
        
        if (response.code === '0' && response.data) {
            if (response.data.officialCode && response.data.qrCodeImage) {
                // 显示官方小程序码
                this.displayOfficialMiniProgramCode(response.data);
            } else {
                // 降级到文本二维码
                await this.generateTextQrCode();
            }
        } else {
            throw new Error(response.msg || '生成失败');
        }
    } catch (error) {
        console.error('生成小程序码失败:', error);
        // 降级到文本二维码
        await this.generateTextQrCode();
    }
}

// 显示官方小程序码
displayOfficialMiniProgramCode(data) {
    this.qrCodeData = data;
    this.qrCodeText = `微信官方小程序码 - 扫码直接跳转`;
    
    // 显示Base64图片
    const canvas = uni.createCanvasContext('qrCanvas', this);
    const img = new Image();
    img.onload = () => {
        canvas.drawImage(img, 0, 0, this.qrSize, this.qrSize);
        canvas.draw();
    };
    img.src = data.qrCodeImage;
    
    uni.showToast({
        title: '✅ 官方小程序码生成成功！',
        icon: 'success',
        duration: 3000
    });
}
```

### 2. 前端场景值处理

```javascript
// 在 visitor-apply.vue 中处理场景值
async handleWechatScene(options) {
    try {
        let scene = options.scene;
        
        if (scene) {
            console.log('🎯 检测到微信小程序码场景值:', scene);
            
            // 解析场景值
            const sceneParams = this.parseSceneValue(scene);
            
            if (sceneParams.bp) {
                // 根据管家后8位手机号获取完整信息
                const butlerInfo = await this.getButlerInfoByPhoneSuffix(sceneParams.bp);
                
                if (butlerInfo) {
                    // 自动填入管家信息
                    this.form.ownerPhone = butlerInfo.phone;
                    
                    // 预设地址信息
                    if (sceneParams.c) {
                        this.form.addressInfo.community = sceneParams.c;
                    }
                    if (sceneParams.b) {
                        this.form.addressInfo.building = sceneParams.b;
                    }
                    if (sceneParams.u) {
                        this.form.addressInfo.units = sceneParams.u;
                    }
                    if (sceneParams.f) {
                        this.form.addressInfo.floor = sceneParams.f;
                    }
                    if (sceneParams.r) {
                        this.form.addressInfo.room = sceneParams.r;
                    }
                    
                    // 构建完整地址
                    this.buildFullAddress();
                    
                    // 显示成功提示
                    uni.showModal({
                        title: '🎯 扫码成功',
                        content: `通过小程序码进入访客申请！

管家信息已自动填入：
姓名：${butlerInfo.name}
电话：${butlerInfo.phone}
地址：${this.form.fullAddress}

请完善其他信息后提交申请。`,
                        showCancel: false,
                        confirmText: '开始申请'
                    });
                }
            }
        }
    } catch (error) {
        console.error('❌ 处理场景值失败:', error);
    }
}
```

## 🎯 部署检查清单

### 1. 后端配置
- [ ] 配置微信小程序AppID和AppSecret
- [ ] 实现access_token获取和缓存
- [ ] 实现小程序码生成接口
- [ ] 处理场景值长度限制（32字符）

### 2. 前端配置
- [ ] 更新API接口调用
- [ ] 处理官方小程序码显示
- [ ] 优化场景值解析逻辑
- [ ] 添加降级处理机制

### 3. 测试验证
- [ ] 测试小程序码生成
- [ ] 验证扫码跳转效果
- [ ] 检查参数自动填入
- [ ] 测试降级机制

## 📱 最终效果

配置完成后，扫码效果将是：
- ✅ 扫码直接打开小程序
- ✅ 自动跳转到访客申请页面
- ✅ 管家信息自动填入
- ✅ 地址信息自动填入
- ✅ 访客只需完善个人信息即可提交

这样就实现了真正的"扫码直接跳转小程序"功能！ 