# 🔧 字段名修复说明

## 📋 问题描述

前端发送JSON请求时使用了下划线命名 `detect_direction`，但后端Java类使用驼峰命名 `detectDirection`，导致JSON反序列化失败。

### 错误信息
```
Unrecognized field "detect_direction" (class com.parkingmanage.vo.PlateRecognitionRequest), not marked as ignorable
```

## ✅ 修复内容

### 前端修改
**文件**: `car-new-demo/pages/demo/plate-scanner.vue`

**修改前**:
```javascript
data: { 
  image: base64Image,
  detect_direction: true  // ❌ 下划线命名
}
```

**修改后**:
```javascript
data: { 
  image: base64Image,
  detectDirection: true   // ✅ 驼峰命名
}
```

### 文档更新
同时更新了以下文档中的API示例：
- `车牌识别错误216201解决方案.md`
- `车牌识别测试指南.md`

## 📋 后端字段定义

`PlateRecognitionRequest` 类的正确字段名：

| 字段名 | 类型 | 描述 | 默认值 |
|--------|------|------|--------|
| `image` | String | Base64编码的图片数据 | 必填 |
| `detectDirection` | boolean | 是否检测图片方向 | true |
| `multiDetect` | boolean | 是否支持多车牌识别 | false |
| `engine` | String | 识别引擎类型 | "baidu" |

## 🎯 正确的API调用示例

```bash
POST http://localhost:8543/api/plate/recognize
Content-Type: application/json

{
  "image": "iVBORw0KGgoAAAANSUhEUgAA...",
  "detectDirection": true,
  "multiDetect": false,
  "engine": "baidu"
}
```

## ✅ 修复结果

修复后，前端发送的JSON字段名与后端Java类字段名完全匹配，不再出现反序列化错误。 