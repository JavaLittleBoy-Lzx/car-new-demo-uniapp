# 🚗 车牌识别功能测试指南

## 📋 功能概述

项目包含完整的车牌识别功能：
- **前端组件**：支持摄像头实时扫描和相册选择
- **后端API**：基于百度AI的车牌识别服务
- **演示页面**：独立的测试演示界面
- **测试接口**：专门的调试和测试API

## 🔧 第一步：配置百度AI密钥

### 1.1 获取百度AI密钥
1. 访问 [百度AI开放平台](https://ai.baidu.com/)
2. 注册/登录账号
3. 创建应用 → 选择"文字识别" → "车牌识别"
4. 在管理中心获取以下信息：
   - App ID
   - API Key
   - Secret Key

### 1.2 配置密钥
编辑 `parking-demo/src/main/resources/application.yml`：

```yaml
# 百度AI配置
baidu:
  ai:
    app-id: 你的百度应用ID
    api-key: 你的百度API密钥
    secret-key: 你的百度Secret密钥
    base-url: https://aip.baidubce.com
    token-url: /oauth/2.0/token
    plate-url: /rest/2.0/ocr/v1/license_plate
    token-cache-minutes: 25
```

## 🚀 第二步：启动服务

### 2.1 启动后端服务
```bash
cd parking-demo
mvn spring-boot:run
```

### 2.2 启动前端服务（可选）
如果要测试小程序界面：
```bash
cd car-new-demo
# 使用HBuilderX打开并运行到浏览器
```

## 🧪 第三步：测试方式

### 方式1：API接口测试

#### 3.1 检查配置
```bash
GET http://localhost:8543/api/plate/test/config
```

#### 3.2 测试连接
```bash
POST http://localhost:8543/api/plate/test/connection
```

#### 3.3 识别车牌（Base64）
```bash
POST http://localhost:8543/api/plate/recognize
Content-Type: application/json

{
  "image": "车牌图片的base64编码",
  "detectDirection": true,
  "multiDetect": false,
  "engine": "baidu"
}
```

#### 3.4 识别车牌（文件上传）
```bash
POST http://localhost:8543/api/plate/recognizeFile
Content-Type: multipart/form-data

file: 选择车牌图片文件
```

### 方式2：Swagger UI测试

访问：`http://localhost:8543/swagger-ui.html`

在"车牌识别接口"和"车牌识别测试接口"部分进行测试

### 方式3：小程序演示页面测试

访问页面：`pages/demo/plate-scanner.vue`

功能包括：
- 📷 摄像头实时识别
- 🖼️ 相册图片识别
- 📊 识别结果展示
- 📝 使用说明

### 方式4：集成组件测试

在其他页面中使用车牌识别组件：

```vue
<template>
  <plate-scanner 
    :auto-recognize="false"
    @recognition-success="handleSuccess"
    @plate-confirmed="handleConfirm"
  />
</template>

<script>
import PlateScanner from '@/components/plate-recognition/plate-scanner.vue'

export default {
  components: { PlateScanner },
  methods: {
    handleSuccess(result) {
      console.log('识别成功:', result)
    },
    handleConfirm(result) {
      console.log('用户确认:', result)
      // 处理确认的车牌号
    }
  }
}
</script>
```

## 📱 第四步：小程序真机测试

### 4.1 准备测试环境
- 使用HBuilderX打开项目
- 配置开发者工具
- 真机调试或模拟器测试

### 4.2 测试功能点
1. **摄像头权限**：确保获取摄像头权限
2. **拍照功能**：测试拍照是否正常
3. **相册选择**：测试从相册选择图片
4. **网络请求**：确保API调用正常
5. **结果展示**：验证识别结果显示

## 🔍 测试用例

### 测试用例1：正常车牌识别
- **输入**：清晰的车牌图片
- **预期结果**：正确识别车牌号、颜色、置信度

### 测试用例2：模糊图片处理
- **输入**：模糊或不清晰的车牌图片
- **预期结果**：返回识别失败或低置信度结果

### 测试用例3：无车牌图片
- **输入**：不包含车牌的图片
- **预期结果**：返回"未识别到车牌"提示

### 测试用例4：多车牌图片
- **输入**：包含多个车牌的图片
- **预期结果**：识别主要车牌（配置为单车牌模式）

## 🐛 故障排除

### 常见问题1：配置错误
**现象**：API返回认证失败
**解决**：检查百度AI密钥配置是否正确

### 常见问题2：网络连接
**现象**：请求超时
**解决**：检查网络连接和防火墙设置

### 常见问题3：图片格式
**现象**：识别失败
**解决**：确保图片格式正确（JPG、PNG等）

### 常见问题4：权限问题
**现象**：摄像头无法启动
**解决**：检查浏览器或小程序摄像头权限

## 📊 性能指标

- **识别准确率**：正常光照下 >95%
- **响应时间**：通常 1-3 秒
- **支持格式**：JPG、PNG、BMP
- **图片大小**：建议 <10MB

## 🔧 高级配置

### 自定义识别参数
在`PlateRecognitionServiceImpl.java`中可调整：
- `multi_detect`：是否支持多车牌
- `detect_direction`：是否检测图片方向

### 缓存优化
- Token缓存：25分钟（可调整）
- 结果缓存：可根据需要添加

## 📝 日志分析

查看日志文件：`/usr/local/parking_demo/logs/ParkingDemo.log`

关键日志：
- 车牌识别开始/结束
- API调用状态
- 错误信息记录

## 🎯 下一步优化

1. **离线识别**：集成本地OCR引擎
2. **批量处理**：支持批量车牌识别
3. **实时流处理**：视频流实时识别
4. **结果缓存**：智能缓存识别结果 